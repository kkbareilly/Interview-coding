package LLDDesign.stackoverflow;

import java.util.Base64;

public class Moderator extends Members{
    public Boolean closeQuestion(Question question);
    public Boolean restoreQuestion(Question question);
}
